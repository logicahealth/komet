<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>sh.isaac</groupId>
		<artifactId>misc</artifactId>
		<version>5.1.1-SNAPSHOT</version>
	</parent>
	<groupId>sh.isaac.misc</groupId>
	<artifactId>importers</artifactId>
	<packaging>maven-plugin</packaging>
	
	<name>ISAAC misc: Importers</name>

	<dependencies>
		<dependency>
			<groupId>sh.isaac.misc</groupId>
			<artifactId>util</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>sh.isaac.misc</groupId>
			<artifactId>vhat-constants</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>sh.isaac.misc</groupId>
			<artifactId>vhat-util</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>sh.isaac.integration</groupId>
			<artifactId>import-utils</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>sh.isaac.core</groupId>
			<artifactId>model</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>sh.isaac.solor</groupId>
			<artifactId>direct-import</artifactId>
			<version>${project.parent.version}</version>
		</dependency>
		<dependency>
			<groupId>com.opencsv</groupId>
			<artifactId>opencsv</artifactId>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi-ooxml</artifactId>
		</dependency>
		<dependency>
			<groupId>javax.xml.bind</groupId>
			<artifactId>jaxb-api</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.maven</groupId>
			<artifactId>maven-plugin-api</artifactId>
			<optional>true</optional> <!-- Don't bring in sub-dependencies -->
		</dependency>
		<dependency>
			<groupId>org.apache.maven.plugin-tools</groupId>
			<artifactId>maven-plugin-annotations</artifactId>
			<optional>true</optional> <!-- Don't bring in sub-dependencies -->
		</dependency>
		<dependency>
			<groupId>org.testng</groupId>
			<artifactId>testng</artifactId>
		</dependency>

		<dependency>
			<groupId>org.apache.jena</groupId>
			<artifactId>jena-core</artifactId>
			<version>3.14.0</version>
			<exclusions>
				<exclusion>
					<groupId>xml-apis</groupId>
					<artifactId>xml-apis</artifactId>
				</exclusion>
				<exclusion>
					<groupId>xerces</groupId>
					<artifactId>xercesImpl</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		
		<!-- Some of the importer mojos do some futzing with log4j inner classes, need this to compile, but those classes 
		aren't used in normal runtime, so not making this a compile / transitive dependency.  The mojo execution runner
		will also provide this at execution time.  -->
		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-core</artifactId>
			<scope>provided</scope>
		</dependency>
		<!-- This gets us nice console logging, so long as we are running ad-hoc tests from the src/test/java package -->
		<dependency>
			<groupId>sh.isaac.core</groupId>
			<artifactId>log-config</artifactId>
			<version>${project.parent.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.antlr</groupId>
			<artifactId>antlr4-runtime</artifactId>
			<version>4.7.1</version>
		</dependency>
		<dependency>
			<groupId>se.liu.imt.mi.snomedct</groupId>
			<artifactId>SnomedCTParser</artifactId>
		</dependency>
		<dependency>
			<groupId>sh.isaac.core</groupId>
			<artifactId>metadata</artifactId>
			<version>${project.parent.version}</version>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<!-- generate code from various xsd files -->
			<plugin>
				<groupId>org.jvnet.jaxb2.maven2</groupId>
				<artifactId>maven-jaxb2-plugin</artifactId>
				<executions>
					<execution>
						<id>generate-CVX-xsd</id>
						<goals>
							<goal>generate</goal>
						</goals>
						<configuration>
							<schemaDirectory>${project.basedir}/src/main/resources/cvx</schemaDirectory>
							<schemaIncludes>
								<include>CVX_version1.xsd</include>
							</schemaIncludes>
							<generatePackage>sh.isaac.convert.mojo.cvx.data</generatePackage>
							<generateDirectory>${project.build.directory}/generated-sources/cvx</generateDirectory>
						</configuration>
					</execution>
					<execution>
						<id>generate-MVX-xsd</id>
						<goals>
							<goal>generate</goal>
						</goals>
						<configuration>
							<schemaDirectory>${project.basedir}/src/main/resources/mvx</schemaDirectory>
							<schemaIncludes>
								<include>MVX_version1.xsd</include>
							</schemaIncludes>
							<generatePackage>sh.isaac.convert.mojo.mvx.data</generatePackage>
							<generateDirectory>${project.build.directory}/generated-sources/mvx</generateDirectory>
						</configuration>
					</execution>
					<execution>
						<goals>
							<goal>generate</goal>
						</goals>
						<id>mif</id>
						<configuration>
							<generatePackage>mifschema</generatePackage>
							<packageLevelAnnotations>false</packageLevelAnnotations>
							<schemaDirectory>${project.basedir}/src/main/resources/hl7v3/mif</schemaDirectory>
							<schemaIncludes>
								<include>xhtml1-strict.xsd</include>
								<include>mif-COMPLETE.xsd</include>
							</schemaIncludes>
						</configuration>
					</execution>
					<execution>
						<goals>
							<goal>generate</goal>
						</goals>
						<id>fhir</id>
						<configuration>
							<generatePackage>fhir</generatePackage>
							<packageLevelAnnotations>false</packageLevelAnnotations>
							<generateDirectory>${project.build.directory}/generated-sources/fhir</generateDirectory>
							<bindingDirectory>${project.basedir}/src/main/resources/fhir/bindings/</bindingDirectory>
							<schemaDirectory>${project.basedir}/src/main/resources/fhir/</schemaDirectory>
							<schemaIncludes>
								<include>fhir-single.xsd</include>
							</schemaIncludes>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>